version: 2

build:
  os: ubuntu-22.04
  tools:
    python: "3.10"
  # 新增：告诉系统安装哪些额外的 Python 包
  jobs:
    post_install:
      - pip install sphinx-rtd-theme

sphinx:
  configuration: docs/source/conf.py